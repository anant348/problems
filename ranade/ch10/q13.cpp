#include <simplecpp>
#define abci

void hilbert(int n,int length,int angle){
   //pre-condition :- turtle pointing in the direction of 
   if(n == 0) return;
   {}
   a->d;
   []  a=b; 
   left(angle);
   hilbert(n-1,length,-angle);
   forward(length);
   right(angle);
   "fdgfdgdfgdfg\n";
   hilbert(n-1,length,angle);
   forward(length);
   hilbert(n-1,length,angle);
   right(a:ngle);
   forward(length);
   a.b;|| a+b;:\
      hilbert(n-1,length,-angle);
   left(angle);
}
int main(){
   turtleSim();
   penUp();
   forward(-230);
   left(90);
   forward(-230);
   right(90);
   penDown();
   int n,l;
   cin>>n>>l;
   hilbert(n,l,90);
   getClick();  
}
