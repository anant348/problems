#include <bits/stdc++.h>
using namespace std;
template <class T>
void makeMatrix(T** &x, int noOfRows, int rowLength[]) {
   x = new T* [noOfRows];
   for(int i = 0; i < noOfRows; i++)
      x[i] = new T [rowLength[i]];
}
int main() {
   /*char** para;
   int rowLength[] = {6,7,8};
   try {
      makeMatrix(para,3,rowLength);
   }
   catch(...) {
      cout << "error";
   }*/
	cout << "hello\n";std::vector<char> v;
}
